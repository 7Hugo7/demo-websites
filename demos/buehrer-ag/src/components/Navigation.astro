---
interface Props {
  companyName: string;
  phone?: string;
  currentPage?: string;
}

const { companyName, phone, currentPage = 'home' } = Astro.props;
---

<nav class="bg-white shadow-md sticky top-0 z-50">
  <div class="max-w-6xl mx-auto px-4 py-4">
    <div class="flex items-center justify-between">
      <a href="/" class="text-xl md:text-2xl font-bold text-[#4D7ABF] hover:text-[#3D6AAF] transition-colors">{companyName}</a>

      <!-- Desktop Navigation -->
      <div class="hidden md:flex items-center gap-6">
        <a href="/" class={`transition-colors ${currentPage === 'home' ? 'text-[#4D7ABF] font-semibold' : 'text-gray-700 hover:text-[#4D7ABF]'}`}>Home</a>
        <a href="/leistungen" class={`transition-colors ${currentPage === 'leistungen' ? 'text-[#4D7ABF] font-semibold' : 'text-gray-700 hover:text-[#4D7ABF]'}`}>Leistungen</a>
        <a href="/verfahren" class={`transition-colors ${currentPage === 'verfahren' ? 'text-[#4D7ABF] font-semibold' : 'text-gray-700 hover:text-[#4D7ABF]'}`}>Spritzverfahren</a>
        <a href="/kontakt" class={`transition-colors ${currentPage === 'kontakt' ? 'text-[#4D7ABF] font-semibold' : 'text-gray-700 hover:text-[#4D7ABF]'}`}>Kontakt</a>
        {phone && (
          <a href={`tel:${phone.replace(/\s/g, '')}`} class="bg-[#4D7ABF] text-white px-4 py-2 rounded-lg hover:bg-[#3D6AAF] transition-colors">
            {phone}
          </a>
        )}
      </div>

      <!-- Mobile Menu Button -->
      <button id="mobile-menu-toggle" class="md:hidden text-gray-700 hover:text-[#4D7ABF] p-2">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>
    </div>

    <!-- Mobile Navigation -->
    <div id="mobile-menu" class="hidden md:hidden mt-4 pb-4 space-y-3">
      <a href="/" class={`block transition-colors ${currentPage === 'home' ? 'text-[#4D7ABF] font-semibold' : 'text-gray-700 hover:text-[#4D7ABF]'}`}>Home</a>
      <a href="/leistungen" class={`block transition-colors ${currentPage === 'leistungen' ? 'text-[#4D7ABF] font-semibold' : 'text-gray-700 hover:text-[#4D7ABF]'}`}>Leistungen</a>
      <a href="/verfahren" class={`block transition-colors ${currentPage === 'verfahren' ? 'text-[#4D7ABF] font-semibold' : 'text-gray-700 hover:text-[#4D7ABF]'}`}>Spritzverfahren</a>
      <a href="/kontakt" class={`block transition-colors ${currentPage === 'kontakt' ? 'text-[#4D7ABF] font-semibold' : 'text-gray-700 hover:text-[#4D7ABF]'}`}>Kontakt</a>
      {phone && (
        <a href={`tel:${phone.replace(/\s/g, '')}`} class="block bg-[#4D7ABF] text-white px-4 py-2 rounded-lg hover:bg-[#3D6AAF] transition-colors text-center">
          {phone}
        </a>
      )}
    </div>
  </div>
</nav>

<script>
  const mobileMenuToggle = document.getElementById('mobile-menu-toggle');
  const mobileMenu = document.getElementById('mobile-menu');

  mobileMenuToggle?.addEventListener('click', () => {
    mobileMenu?.classList.toggle('hidden');
  });
</script>
