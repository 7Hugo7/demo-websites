---
interface Props {
  title: string;
  subtitle: string;
  ctaText?: string;
  ctaLink?: string;
  backgroundImage?: string;
}

const { title, subtitle, ctaText, ctaLink, backgroundImage } = Astro.props;
---

<section class="relative overflow-hidden text-white py-24 px-4">
  {backgroundImage && (
    <div class="absolute inset-0">
      <img src={backgroundImage} alt="" class="w-full h-full object-cover" />
      <div class="absolute inset-0 bg-gradient-to-br from-blue-900/70 to-blue-800/60"></div>
    </div>
  )}
  {!backgroundImage && (
    <div class="absolute inset-0 bg-gradient-to-br from-blue-600 to-blue-800"></div>
  )}
  <div class="relative max-w-6xl mx-auto text-center">
    <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold mb-6 drop-shadow-lg">{title}</h1>
    <p class="text-xl md:text-2xl mb-8 drop-shadow-md">{subtitle}</p>
    {ctaText && ctaLink && (
      <a
        href={ctaLink}
        class="inline-block bg-white text-blue-600 font-semibold px-8 py-4 rounded-lg hover:bg-blue-50 transition-colors shadow-lg"
      >
        {ctaText}
      </a>
    )}
  </div>
</section>
