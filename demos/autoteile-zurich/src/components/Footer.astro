---
interface Props {
  companyName: string;
  year?: number;
}

const { companyName, year = new Date().getFullYear() } = Astro.props;
---

<footer class="bg-gray-900 text-white py-8 px-4">
  <div class="max-w-6xl mx-auto text-center">
    <div class="mb-6 p-3 md:p-4 bg-yellow-900/30 border border-yellow-500/50 rounded-lg">
      <p class="text-yellow-300 font-semibold text-sm md:text-base">⚠️ Hinweis / Disclaimer</p>
      <p class="text-yellow-200 text-xs md:text-sm mt-2">
        Dies ist keine offizielle Website des Unternehmens. Diese Seite dient ausschließlich zu Demonstrationszwecken.
      </p>
    </div>
    <p class="text-gray-400 text-sm md:text-base">&copy; {year} {companyName}. Alle Rechte vorbehalten.</p>
    <div class="mt-4 space-x-4 text-sm md:text-base">
      <a href="#" class="text-gray-400 hover:text-white transition-colors">Impressum</a>
      <a href="#" class="text-gray-400 hover:text-white transition-colors">Datenschutz</a>
    </div>
  </div>
</footer>
